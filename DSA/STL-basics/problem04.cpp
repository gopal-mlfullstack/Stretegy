#include <iostream>
#include <string>
#include <algorithm>
using namespace std;
// leetcode 557 -- Reverse Words in a string III
/*
 Problem Statement (in your own words):
 You are given a sentence (string s) that may contain multiple words separated by single spaces.
 You have to reverse each word individually, but keep the original word order and spaces the same.
 example input: Let's take LeetCode contest
 example output: s'teL ekat edoCteeL tsetnoc
*/

int main(){

    // string str = "Let's take LeetCode contest";
    // int start = 0;
    // for (int i = 0; i <= str.length(); i++){
    //     if(i == str.length() || str[i] ==  ' '){
    //         reverse(str.begin() + start, str.begin() + i);
    //     start = i + 1;
    // }
    // }
    // cout << str << endl;
    //

    //---------------------------------
    //





    return 0;










}
