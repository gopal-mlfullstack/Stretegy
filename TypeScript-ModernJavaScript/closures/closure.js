// Code Blocks
// If a variable isdeclared inside a code block {...}, it's only visible inside that block.
// for example:
{
  // doing some job with local variables that should not be seen outside
  let message = "Hello my name is Gopal Mahato!"; // Only visible in this block
  alert(message); // it will just returns the message
}
